<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GoTour.MVVM.View.MenuView"
             xmlns:vm="clr-namespace:GoTour.MVVM.ViewModel"
             NavigationPage.HasNavigationBar="false"
             >

    <ContentPage.BindingContext>
        <vm:MenuViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Image Source="https://firebasestorage.googleapis.com/v0/b/gotour-98c79.appspot.com/o/ProfilePic%2F286181073.jpg"/>
            <Button Grid.Row="1" Text="Choose Image!" Command="{Binding AddImageCommand}" />
            <Button Grid.Row="2" Text="Choose  cc ba m !" Command="{Binding PushDatacommand}" />

        </Grid>
    </ContentPage.Content>
</ContentPage>